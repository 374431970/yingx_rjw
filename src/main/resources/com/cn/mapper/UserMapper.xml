<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cn.dao.UserDao">
    <select id="findAllBySex" resultType="com.cn.po.CityPO">
        select city name,count(id) value
        from yx_user where sex=#{sex} group by city
    </select>
    <select id="findAllByMonth" resultType="java.lang.Integer">
        select count(id) counts
        from yx_user where sex=#{sex} and month(create_date) = #{month}
    </select>
</mapper>